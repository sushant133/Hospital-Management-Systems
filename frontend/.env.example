# The client calls the API at a same-origin /api/v1 path — in dev that is the
# Vite proxy (vite.config.js), in Docker it is the nginx proxy (nginx.conf).
# Nothing secret belongs in here: everything Vite inlines ships to the browser.

# Dev server port (must match CLIENT_ORIGIN in backend/.env)
VITE_PORT=5180
